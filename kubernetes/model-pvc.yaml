apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-pvc
spec:
  accessModes:
    - ReadWriteMany  # Allows multiple pods to read AND write
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client  # NFS storage class for ReadWriteMany support